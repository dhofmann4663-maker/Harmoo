import React from "react";
import { PieChart, Pie, Cell, Tooltip, ResponsiveContainer } from "recharts";

const tokenomicsData = [
  { name: "Community / Public", value: 70, color: "#60A5FA" },
  { name: "Charity Wallet", value: 10, color: "#34D399" },
  { name: "Team / Dev Reserve", value: 10, color: "#FBBF24" },
  { name: "Marketing & Growth", value: 5, color: "#F472B6" },
  { name: "Partnerships / Future Utility", value: 5, color: "#A78BFA" },
];

export default function Home() {
  return (
    <div className="min-h-screen bg-gray-50 text-gray-800">
      {/* Hero Section */}
      <header className="bg-gradient-to-r from-blue-400 to-purple-500 text-white py-20 px-6 text-center">
        <h1 className="text-5xl font-extrabold mb-4">$HARMOO</h1>
        <p className="text-xl max-w-2xl mx-auto">
          The meme coin that saves tails 🐾 Inspired by Harmoo the Pomeranian, built
          to support Noah’s Hope Animal Rescue.
        </p>
      </header>

      {/* Mission Section */}
      <section className="py-16 px-6 max-w-5xl mx-auto text-center">
        <h2 className="text-3xl font-bold mb-6">Our Mission</h2>
        <p className="text-lg max-w-3xl mx-auto">
          $HARMOO is more than just a meme coin – it’s a movement. Every trade
          contributes to helping animals in need, with a portion of tokens dedicated
          to supporting <strong>Noah’s Hope Animal Rescue</strong>. Harmoo’s pampered life is our
          mascot’s story, and with your help, we can share that comfort with pets
          who need it most.
        </p>
      </section>

      {/* Tokenomics Section */}
      <section className="py-16 px-6 bg-white shadow-inner">
        <h2 className="text-3xl font-bold text-center mb-6">Tokenomics</h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-10 items-center max-w-6xl mx-auto">
          {/* Pie Chart */}
          <ResponsiveContainer width="100%" height={300}>
            <PieChart>
              <Pie
                data={tokenomicsData}
                cx="50%"
                cy="50%"
                labelLine={false}
                outerRadius={100}
                fill="#8884d8"
                dataKey="value"
              >
                {tokenomicsData.map((entry, index) => (
                  <Cell key={`cell-${index}`} fill={entry.color} />
                ))}
              </Pie>
              <Tooltip />
            </PieChart>
          </ResponsiveContainer>

          {/* Table */}
          <div>
            <table className="w-full border border-gray-200 rounded-lg overflow-hidden">
              <thead className="bg-gray-100">
                <tr>
                  <th className="p-3 text-left">Allocation</th>
                  <th className="p-3 text-left">%</th>
                </tr>
              </thead>
              <tbody>
                {tokenomicsData.map((row, i) => (
                  <tr key={i} className="border-t">
                    <td className="p-3">{row.name}</td>
                    <td className="p-3">{row.value}%</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
      </section>

      {/* How to Buy Section */}
      <section className="py-16 px-6 text-center max-w-4xl mx-auto">
        <h2 className="text-3xl font-bold mb-6">How to Buy $HARMOO</h2>
        <ol className="text-left list-decimal list-inside space-y-3 text-lg">
          <li>Download a Solana wallet (Phantom or Solflare).</li>
          <li>Buy SOL from an exchange and transfer it to your wallet.</li>
          <li>
            Visit <a href="https://pump.fun" className="text-blue-500 underline">Pump.fun</a> and search for <strong>$HARMOO</strong>.
          </li>
          <li>Swap SOL for $HARMOO directly on Pump.fun or later via Raydium.</li>
        </ol>
      </section>

      {/* Charity Transparency Section */}
      <section className="py-16 px-6 bg-gray-100 text-center">
        <h2 className="text-3xl font-bold mb-6">Charity Transparency</h2>
        <p className="max-w-3xl mx-auto text-lg mb-4">
          We believe in full transparency. Our dedicated charity wallet ensures that
          every $HARMOO holder can see the direct impact of their support.
        </p>
        <p className="font-mono text-blue-600 break-all">WALLET_ADDRESS_HERE</p>
      </section>

      {/* Community Section */}
      <footer className="py-10 px-6 text-center bg-gradient-to-r from-purple-500 to-blue-400 text-white">
        <h2 className="text-2xl font-bold mb-4">Join the Community</h2>
        <div className="flex justify-center space-x-6">
          <a href="#" className="hover:underline">Twitter</a>
          <a href="#" className="hover:underline">Telegram</a>
          <a href="#" className="hover:underline">TikTok</a>
          <a href="#" className="hover:underline">Instagram</a>
        </div>
        <p className="mt-6 text-sm">© 2025 HARMOO. All rights reserved.</p>
      </footer>
    </div>
  );
}
